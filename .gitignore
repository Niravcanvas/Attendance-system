# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc

# Virtual environment
venv/
env/
.venv/

# Environment variables — never commit real secrets
.env
# .env.example is safe to commit ✓

# MongoDB replaces SQLite — delete these if they exist
*.db
*.sqlite
*.sqlite3
attendance.csv

# Face data — large binary files, store/backup separately
dataset/
encodings/*.npy

# Keep the JSON metadata files (small, useful to track)
!encodings/index.json
!encodings/subjects.json
!encodings/teachers.json

# Runtime-generated files
logs/
uploads/

# Legacy/unused files — delete these from your project
attendance_helpers.py
embedding_model.py
encode_faces_insightfaces.py
reset_attendance.py

# macOS
.DS_Store

# IDEs
.vscode/
.idea/
*.swp
*.swo